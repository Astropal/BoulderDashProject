package contract.model;

public enum Permeability {
	
	/** The blocking. */
    BLOCKING,
    /** The penetrable. */
    PENETRABLE,
    /** The pushable. */
    PUSHABLE,
    /** The destructible. */
    DESTRUCTIBLE,
    /** The removeable. */
    REMOVEABLE,
    /** The killable. */
    KILLABLE,
    /** The finishable. */
    FINISHABLE,
    /** The killable2 (the second enemy). */
    KILLABLE2;
}
