package contract.model;

public enum Permeability {
	
	 /** The blocking. */
    BLOCKING,
    /** The penetrable. */
    PENETRABLE,
    
    PUSHABLE,
    
    DESTRUCTIBLE,
    
    REMOVEABLE;
}
